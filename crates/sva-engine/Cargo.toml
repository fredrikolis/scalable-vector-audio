# Concern: declares the package and its dependencies on the document model and the law crate | Non-concern: the deny edges barring sva-cli's reach into it (.annotated-tree.toml) | IO: none
[package]
name = "sva-engine"
description = "Renders a resolved graph into per-node buffers a query can be asked of"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
sva-fingerprint = { workspace = true }
sva-ast = { workspace = true }
sva-formula = { workspace = true }
sva-samples = { workspace = true }

[dev-dependencies]
sva-ast = { workspace = true }

[build-dependencies]
sva-fingerprint = { workspace = true }
